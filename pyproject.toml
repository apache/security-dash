[project]
name = "security-dashboard-frontend"
version = "0.1.0"
license = "Apache-2.0"

dependencies = [
  "asfpy[aioldap]",
  #"asfquart~=0.1.12",
  "asfquart @ git+https://github.com/raboof/infrastructure-asfquart@oauth-support-client-id-and-secret",
  "hypercorn",
  "pydantic~=2.11",
]

[tool.pytest.ini_options]
norecursedirs = ["notes"]

